const { lineCount } = require('./line-count');

describe('line count', () => {
  it.todo('for file —— line-count.js —— should be 12');

  it.todo('for file —— is-not-there.js —— should be problem reading file: ' +
      'is-not-there.js');
});